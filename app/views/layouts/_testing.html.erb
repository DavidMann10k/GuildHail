<%= content_tag :h3, "I AM #{current_user.name}"  if signed_in? %>
<%= debug(params) if Rails.env.development? %>
<ul>
	<!--<% request.env.each do |item| %>
	    <li><%= item[0] %> : <%= item[1] %></li>
	<% end %>-->
</ul>
	<%= content_tag :h3, params.inspect %>
	<%= content_tag :h3, debug(params) %>
	