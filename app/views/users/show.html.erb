<h1><%= @user.name %></h1>
<h2>Memeber Since</h2>
<%= @user.created_at %>
<% unless @user.alliances.empty? %>
<h2>Member Of</h2>
	<% @user.alliances.each do |a| %>
		<%= a.name %> since <%= l a.membership.created_at, :format => :short %>
	<% end %>
<% end %>